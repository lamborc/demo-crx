'use strict'

const browser = require('webextension-polyfill')
//const { optionDefaults } = require('../lib/options')

document.addEventListener('DOMContentLoaded',async () => {
  console.log('document>add EventListener')
})
